<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.farras.kalenderkeuangan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KalenderKeuangan">
        <activity
            android:name=".KantorActivity"
            android:exported="true" />
        <activity
            android:name=".PribadiActivity"
            android:exported="true"
            android:label="@string/title_activity_pribadi"
            android:theme="@style/Theme.KalenderKeuangan.NoActionBar" />
        <activity
            android:name=".ui.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>